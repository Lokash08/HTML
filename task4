!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Block vs Inline Elements</title>
  <style>
    .block-example {
      background-color: lightblue;
      margin: 10px 0;
      padding: 10px;
    }
    .inline-example {
      color: red;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <h1>Block-level vs Inline Elements</h1>
  
  <!-- Block-level elements -->
  <div class="block-example">
    <h2>This is a heading (block)</h2>
    <p>This is a paragraph (block-level element). It starts on a new line.</p>
  </div>

  <!-- Inline elements inside block -->
  <p>Here is an example with <span class="inline-example">inline text using span</span>
  inside a paragraph.</p>
  
  <!-- Anchor as inline -->
  <p>Visit <a href="https://www.google.com">Google</a> (inline link).</p>

  <!-- Another block -->
  <div class="block-example">
    This is a <div>division</div> (block-level element).
  </div>

</body>
</html>
